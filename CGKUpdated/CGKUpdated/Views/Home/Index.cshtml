@model IEnumerable<CGKUpdated.Models.ImagePair>

@{
    ViewData["Title"] = "Home Page";
    /*
    Random r = new Random((int) DateTime.Now.Ticks);
    //TODO: More Efficient Random Access
    ImagePair[] rows = Model.ToArray();
    string randomEntry = rows[r.Next(rows.Length)].title;
    */
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>

    <a asp-controller="ImagePairs" asp-action="Create">
		<input type="button" value="Create" class="button" />
    </a>
    <a asp-controller="ImagePairs" asp-action="Index">
		<input type="button" value="Browse" class="button" />
    </a>
    <!--
    <a asp-controller="ImagePairs" asp-action="/Details/...">
		<input type="button" value="Random" class="button" />
    </a>
    -->
</div>

<style>
    .button {
        position: relative;
        display: inline-block;
        outline: none;
        cursor: pointer;
        font-weight: 600;
        border-radius: 3px;
        padding: 12px 24px;
        border: 0;
        color: #000021;
        background: #1de9b6;
        line-height: 1.15;
        font-size: 16px;
    }

    button:hover {
        transition: all .1s ease;
        box-shadow: 0 0 0 0 #fff, 0 0 0 3px #1de9b6;
    }
</style>